type Film = {
    id: number,
    title: string
    description: string
    release_date: string
    image_filename 	: string
    runtime: number
    director_id: number
    genre_id: string
    age_rating:string
}